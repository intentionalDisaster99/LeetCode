vector<int> rearrangeArray(vector<int>& nums) {
        
        // I think I will be creating a new vector to return
        // The way I am going to do this will be really slow, but nice and simple.
        vector<int> output;

        // O(n^2), the fastest algorithm ever
        for (int i = 0; i < nums.size(); i)



}